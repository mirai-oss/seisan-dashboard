# 精算ダッシュボード 引き継ぎ書（新しいスレッド用）

このドキュメントは、このプロジェクトの作業を**別の会話（新しいスレッド）に引き継ぐ**ために書かれています。冒頭から読めば、コード全部を読み直さなくても現状と次にやることが分かるようにしてあります。

---

## 1. これは何か（30秒で把握）

飲食店グループの「業務委託先への精算書」を扱うWebダッシュボード。入力チェック・PDF発行・メール送信・振込管理・法人別集計・年間推移までを1つの画面で完結させる。

- **裏側のデータ**: Googleスプレッドシート（既存の精算業務で使われていたもの。今もこれが正）
- **API**: Google Apps Script（GAS）が軽量JSON APIとして動く
- **画面**: GitHub Pagesで配信する独立した静的サイト（Googleログイン不要）
- **利用者**: 本部（全機能）／マスター（本部の上位・ロック解除等の特権）／委託先（自店舗の閲覧のみ）

## 2. 重要なURL・ID

| 項目 | 値 |
|---|---|
| 画面（本番URL） | https://mirai-oss.github.io/seisan-dashboard/ |
| GAS Webアプリ（API） | https://script.google.com/macros/s/AKfycbzwYN9uSEtcJHSKSVQCoQOrllhO7G6gR-E4dvP-V4o_VdGXr9VQx2mbYYPNyNEFSQCiKg/exec （`?action=ping`でバージョン確認可） |
| GitHubリポジトリ | https://github.com/mirai-oss/seisan-dashboard （**Public**。理由は後述） |
| 対象スプレッドシート | `1GlJRMjKTMjqr22xo52xI3AdWJB2UZ_VlRDORqYOnXv4`（精算書 入力シート） |
| 売上連携元スプレッドシート | `1OuaAQBeXHxJZtDXEbQx-V7w56fCWW5jpDmZvBpkfIbQ`（【サーバー】ダッシュボード「分析_日別店舗」。現金取込・売上照合で参照） |
| ローカル作業ディレクトリ | `/Users/mirai/Claude/seisan-dashboard/` |
| 現在のコード内バージョン表記 | `v5.6-sso`（`gas/SeisanDashboard.gs` 冒頭 `SD_VERSION`。直近のコミットでバージョン文字列は上げていないので、実際のコードはこれより新しい点に注意） |

## 3. なぜこの構成か（誤って「元に戻さない」ように）

最初はGASのHtmlServiceで画面ごと配信していたが、**Google Workspace管理者のApps Script制限**により、組織アカウントでログイン中のスマホから「現在、ファイルを開くことができません」と出て開けない事象が発生した。これを回避するため、**画面をGoogleと無関係なGitHub Pagesに切り出し、GASはデータ授受専用のJSON APIに徹する**構成に全面移行した（売上ダッシュボード tori-dashboard と同じ考え方）。

このためリポジトリは **Public** にしてある（Privateだと現在のGitHubプランでPagesが使えないため）。**コードに機密情報は一切ハードコードしていない**（スプレッドシートID、Drive フォルダID、初期パスワード、Lark Webhook URL等はすべて「設定_外部連携」「権限_精算ダッシュボード」シート側で管理）。新しい設定値を追加する時もこの原則を必ず守ること。

## 4. ファイル構成

```
seisan-dashboard/
├── index.html                          画面本体（GitHub Pagesで配信）
├── gas/
│   ├── SeisanDashboard.gs              GASバックエンド（JSON APIのみ）
│   └── SeisanDashboard_コピー用.txt     ↑と同一内容。Apps Scriptへの貼り付け用
├── preview.html                        サンプルデータのfetchモック入りプレビュー（開発確認専用。gitに含む）
├── README.md
├── 設定手順.md                          セットアップ・更新手順（ユーザー向け）
└── 引き継ぎ書.md                        このファイル
```

**Apps Scriptに貼り付けるのは `SeisanDashboard.gs` の1ファイルだけ**（HTMLファイルは不要になった。以前の版はdashboard.htmlも貼っていたが廃止）。

## 5. 更新・デプロイの手順（毎回これを守る）

1. `gas/SeisanDashboard.gs` と/または `index.html` を編集
2. **両方とも構文チェックする**（人間用の目視ではなく機械的に）
   ```bash
   cp gas/SeisanDashboard.gs /tmp/_c.js && node --check /tmp/_c.js
   python3 -c "import re; src=open('index.html',encoding='utf-8').read(); m=re.search(r'<script>\n(/\* GAS.*?)</script>',src,re.S); open('/tmp/_i.js','w').write(m.group(1))"
   node --check /tmp/_i.js
   ```
3. `index.html` を編集したら **`preview.html` を最新の `index.html` から作り直してから**プレビューで確認する（過去に何度か「preview.htmlが古いままで検証していた」というミスが起きているので注意）。作り方:
   ```bash
   python3 -c "
   import re
   prev = open('preview.html', encoding='utf-8').read()
   m = re.search(r'(<script>\n\(function\(\)\{.*?\}\)\(\);\n</script>\n)', prev, re.S)
   mock = m.group(1)
   src = open('index.html', encoding='utf-8').read()
   out = src.replace('<script>\n/* GAS', mock + '<script>\n/* GAS', 1)
   banner = '<body>\n<div style=\"background:#1f2430;color:#fff;font-size:12px;padding:8px 16px;text-align:center\">🔍 プレビュー（サンプル）</div>'
   out = out.replace('<body>', banner, 1)
   open('preview.html','w',encoding='utf-8').write(out)
   "
   ```
   Browser paneの`seisan-preview`（`.claude/launch.json`に定義済み、port 8899）で `preview.html?v=N`（キャッシュ回避のためクエリを毎回変える）を開いて確認する。ログインは `master` / `honbu` / `fam` などID文字列に応じてモックが役割を切り替える（`preview.html`内の`sd_login`モック参照）。
4. `gas/SeisanDashboard.gs` を編集したら `gas/SeisanDashboard_コピー用.txt` にも同じ内容をコピーするのを忘れない:
   ```bash
   cp gas/SeisanDashboard.gs gas/SeisanDashboard_コピー用.txt
   ```
5. `git add -A && git commit -m "..." && git push`（画面側はこれだけでGitHub Pagesに数分で反映される）
6. **GAS側はユーザーに以下をお願いする必要がある**（Apps Scriptの外部からは編集・デプロイ操作ができないため）:
   - `SeisanDashboard_コピー用.txt` の中身を全部コピーしてApps Scriptの`SeisanDashboard`ファイルに貼り付け・保存
   - **デプロイ → デプロイを管理 → ✏️編集 → バージョン「新バージョン」→ デプロイ**（「新しいデプロイ」を選ぶとURLが変わってしまうので厳禁。既存デプロイの編集で「新バージョン」を選ぶこと）
   - `?action=ping` でバージョン文字列が更新されたか確認してから作業完了とする

## 6. 権限モデル

3段階: **マスター > 本部 > 委託先**（`権限_精算ダッシュボード`シートで管理）。

- 本部: 全店舗の閲覧・入力・発行・メール送信ができる
- マスター: 本部の権限に加えて、**振込済みロック中のデータの編集・振込済み解除**ができる唯一の権限。マスター権限の付与はマスターのみが行える（権限昇格防止）
- 委託先: 自分の担当店舗のみ閲覧。N-style収益は非表示。添付・PDFの閲覧は可、編集・削除は不可

**振込済みロック**: ある店舗のある月が「振込済み」チェックされると、その店舗・月のデータ編集（追加・修正・削除・添付・一括計上・現金取込・定期費目セット）はマスター以外できなくなる（サーバー側 `sd_requireUnlocked_` / `sd_requireMaster_` で強制、UIでもボタン無効化）。

既存環境で新規にマスターアカウントを作るには、Apps Scriptエディタで `sd_createMasterAccount` を1回実行する（ランダムなパスワードが実行ログに1回だけ出る）。

## 7. SSO統合について（★別セッションの作業・要注意）

`gas/SeisanDashboard.gs` の `sd_supaLogin` と `index.html` の「統合アカウント（ポータル/日報）でログイン」機能は、**このセッションでは実装していない**。git log で確認したところ、2026-08-05 に別スレッド（別のClaude Codeセッション）で追加されたもの:

- `sd_supaLogin(stoken)`: Supabaseのアクセストークンを `/auth/v1/user` で検証し、権限シートG列「メール」と突き合わせてログインさせる
- ns-portal（日報・ポータル）と同一のSupabaseプロジェクトを使い、同一オリジンのlocalStorageセッションを読んで自動ログインを試みる（`trySilentPortalLogin`）
- 委託先アカウントは統合ログインの対象外（サーバー側で明示的に拒否）

**このログイン周りのコードを触る前に、必ず現物を読み直すこと。** このプロジェクトはこれまでも「前回のセッションが把握していないコードが増えている」ことが複数回あった（このSSO機能自体がその一例）。行番号やコード内容を記憶や過去のメモリ記録だけで信じず、`grep`/`Read`で現状を確認してから編集する。

## 8. 現在進行中の課題（★ここが一番重要・次にやること）

### 8-1. 速度問題の調査（進行中）

ユーザーから「入力・修正・添付アップロードのたびに10〜15秒かかる、ストレス」との報告があり、調査・対処を進めている。

**分かったこと（実測ベース）:**
1. 修正・削除そのものより、**その直後に必ず走る画面の再読み込み（`sd_getDashboard`）が慢性的に重い**（変更操作なしの「画面読込」だけでも9〜19秒）
2. サーバー時間が往復時間の7〜8割を占める → 通信の問題ではなく **GAS内部の処理が遅い**
3. 何度読み込んでも速くならない → **キャッシュが効いていなかった**

**特定した根本原因（直近コミット ad2e6c8 で修正済み・★まだユーザーの再デプロイ・再テスト待ち）:**
`sd_readRowsCached_` が「JSON化後95KBを超える店舗はキャッシュに保存しない」実装になっており（CacheServiceの1キー100KB上限を回避するための雑な条件）、明細が多い店舗（特に秋葉原 肉寿司）は**キャッシュへの保存自体が毎回黙って失敗**し、常にシート全読みしていた。→ 複数キーに分割保存する方式に変更してこの上限を回避し、TTLも3分→10分に延長した。

**あわせて実装した可視化:**
- 画面上部に常設の「⏱ 処理速度ログ」パネル（開発者ツール不要、クリックで開閉）。ユーザーは非技術者でconsole.logの場所を知らなかったため、画面表示が必須だった
- `sd_getDashboard`に店舗ごとの個別タイマー（`店舗:店舗名`）を追加し、パネルにサーバー内訳をそのまま表示するようにした

**★次にやること:**
ユーザーに以下を依頼し、結果を見て判断する:
1. `SeisanDashboard_コピー用.txt`（コミット ad2e6c8 の内容）を貼り直して新バージョンでデプロイ
2. 何回か画面読込・修正等を試してもらう
3. 「⏱ 処理速度ログ」パネルのスクリーンショットを共有してもらう
4. **キャッシュヒット時に速くなっていれば**: 一旦解決。Supabase移行は不要と判断してよい
5. **それでも遅い店舗がある場合**: パネルの店舗別内訳（`店舗:店舗名:X.Xs`）を見て、その店舗固有の原因（明細の異常な多さ、DBシート構造の問題等）を追加調査
6. **添付ファイル（Google Drive）が引き続き遅い場合**: Drive側のフォルダ検索（月フォルダ→店舗フォルダの2段階、`sd_folderFor_`）は既に10分キャッシュ済みだが、それでも遅ければ添付だけSupabase Storageへ切り出す価値がある（下記8-2）

### 8-2. Supabase移行の検討（ユーザー提案・まだ合意していない）

ユーザーは「他のシステム（日報・ポータル）でSupabaseを使っているので、精算ダッシュボードの数字データ・添付写真もSupabaseに切り替えれば速いのでは」と提案している。回答した見解（実装はまだしていない）:

- **数字データ（明細）の全面移行**: Postgresは確実に速いが、精算計算・ロック・PDF生成など今GASに書いてある業務ロジックを全部書き直す規模の変更になる。まずは8-1のキャッシュ修正の効果を実測してから判断すべき、と伝えてある
- **添付ファイルだけSupabaseStorageに切り出す**: Google Driveのフォルダ検索は構造的に遅く、数字データほど複雑なロジックが絡まないので、切り出しの手間は小さい。8-1の結果次第で検討する価値がある、と伝えてある
- 技術的な接続口はある程度既にある（7章のSSO統合で同じSupabaseプロジェクトに繋がっているため）

**新しいスレッドでこの続きをやる場合、8-1の実測結果が出ているかをまず確認し、出ていなければユーザーに催促するところから始めること。** 「Supabase化する/しない」を実測なしで先に進めない。

## 9. 既知の注意点・落とし穴（再発防止）

- **GASの「保存」だけでは公開URLに反映されない**。必ず「デプロイを管理→編集→新バージョン→デプロイ」が要る。ユーザーが何度もこれで詰まっている
- **貼り付け間違いが頻発している**（ファイルパス文字列やJSON応答をそのまま貼ってしまう等）。貼り付け後は1行目を確認するようユーザーに毎回念押しすること
- **`preview.html`は自動で最新化されない**。`index.html`を編集したら都度作り直す（5章参照）
- Browser paneのスクリーンショットが稀にフリーズすることがある。JSでのDOM検証（`innerHTML`取得等）で代替できる
- 実データの検証は慎重に。テスト用の書き込みをする場合は「【テスト】」等と分かる名前を付け、影響範囲をユーザーに事前確認する
- ユーザーは非技術者。ログの場所・デプロイ手順など、毎回具体的な手順で案内すること（「コンソールを見てください」ではなく「画面のここをクリックしてください」）

## 10. 参照先

- このプロジェクトの詳細な作業履歴（v1〜現在までの全変更点）は、このClaude Code環境の自動メモリに記録されている:
  `/Users/mirai/.claude/projects/-Users-mirai-Claude/memory/seisan-dashboard-project.md`
  （※この環境固有のメモリなので、別環境からこの引き継ぎ書だけを渡す場合は参照できない。その場合はこのファイルとgitのコミットログが正）
- 姉妹プロジェクト（経営ダッシュボード）: https://github.com/mirai-oss/tori-dashboard 。将来的にDB_精算シート経由での連携を予定している（README.md参照）
