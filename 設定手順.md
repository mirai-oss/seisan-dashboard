# 精算ダッシュボード 設定・更新手順

v5.0からアーキテクチャが変わりました。**画面（見た目）はGitHub Pages、データ処理はGoogle Apps Script** の2部構成です。これによりスマホ（会社のGoogleアカウントでも）Googleログインの壁なしで開けるようになります。

---

## 初回セットアップ

### 1. GAS側（データ処理API）を貼り付け

1. 対象スプレッドシート → 拡張機能 → Apps Script
2. スクリプトファイルを新規作成し、名前を `SeisanDashboard` にする
3. **`gas/SeisanDashboard_コピー用.txt` の内容を全部貼り付け**（このファイル1つだけでOK。HTMLファイルは不要になりました）
4. 💾 保存
5. 関数選択で `sd_authorize` を選んで実行 → 権限を承認
6. 実行ログを開く（表示 → 実行数、または直近の実行をクリック）→ **「本部アカウントの初期パスワードを発行しました: honbu / ××××××××」** をメモする（初回のみ表示されます）

### 2. デプロイ

1. 右上 **デプロイ → 新しいデプロイ**
2. 種類: **ウェブアプリ**
3. 次のユーザーとして実行: **自分**
4. アクセスできるユーザー: **全員**
5. デプロイ → 表示された **`.../exec` のURL** をコピー

### 3. 画面側（GitHub Pages）にURLを設定

1. `index.html` を開き、`var GAS_API_URL = '...'` の行を、上でコピーしたURLに書き換える
2. GitHubにコミット＆push
3. 数分待つと `https://mirai-oss.github.io/seisan-dashboard/` に反映される

### 4. 初回ログイン

1. 上記URLを開く
2. ID: `honbu` / パスワード: 手順1でメモした初期パスワード でログイン
3. **⚙️設定タブから、すぐにパスワードを変更してください**
4. 委託先アカウントも同じ⚙️設定タブから発行できます

---

## コードを更新するとき（毎回の手順）

### GAS側（データ処理）を変更した場合

1. `gas/SeisanDashboard_コピー用.txt` の内容で、Apps Scriptの `SeisanDashboard` を全置き換え
2. 保存
3. デプロイ → デプロイを管理 → 編集 → バージョン「新バージョン」→ デプロイ
   （**URLは変わりません**。「新しいデプロイ」を作らない限り同じURLのままです）

### 画面側（index.html）を変更した場合

1. GitHubにpushするだけ（GAS側の再デプロイは不要）
2. 数分でGitHub Pagesに反映

### 動作確認

- API疎通確認: `GAS_API_URL?action=ping` を直接開いて `{"ok":true,"ver":"..."}` が出ればGAS側は正常
- 画面確認: `https://mirai-oss.github.io/seisan-dashboard/` を開いてログイン画面が出ればOK

---

## ログインアカウント

`権限_精算ダッシュボード` シート、または本部ログイン後の **⚙️設定** タブから追加・編集できます。

| ログインID | 権限 | 担当店舗 |
|---|---|---|
| honbu | 本部 | 全店 |
| （委託先ごとに発行） | 委託先 | 担当店舗名 |

委託先には、**画面のURL**（`https://mirai-oss.github.io/seisan-dashboard/`）と発行したID/PWを伝えます。URLは全アカウント共通で、ログインした権限に応じて見える範囲が変わります。

---

## 自動で作られる管理シート（すべて編集可）

`設定_精算ダッシュボード`（店舗↔DB対応・必須費目・売上シート店舗名）／ `設定_外部連携`（売上シートID・添付フォルダID・発行元情報・リマインド送信先など）／ `権限_精算ダッシュボード`（アカウント）／ `法人設定_精算ダッシュボード`（振込先口座）／ `定期費目_精算ダッシュボード`（毎月自動セットする費目）／ `振込管理_精算ダッシュボード`（振込済み記録）／ `発行ログ_精算ダッシュボード`（送信・添付履歴）

**初回起動時にこれらのシートが自動生成されるので、`設定_外部連携`シートの各項目（売上スプレッドシートID、添付フォルダID、発行元の会社名・住所など）を埋めてください。**

## 自動処理（「🕘 毎月1日の自動セットをON」ボタンで有効化）

- 毎月1日 朝9時: 前月分（精算対象月）へ定期費目を自動セット
- 毎月18日 朝9時: 振込期限（20日）リマインドを「設定_外部連携」の**リマインド送信先**へ送信（入力漏れ疑いの店舗・項目つき。未設定ならスキップされます）
