# 精算ダッシュボード 設定・更新手順

## ★ スマホで開けない問題の直し方（最重要）

委託先がスマホ（特にGmailアプリ内のブラウザ）でメールのリンクを開くと、Googleログインを求められて開けないことがあります。原因は**デプロイの「アクセスできるユーザー」設定**です。以下に変更すると、Googleログイン不要で、自前のID/PWログイン画面だけで入れるようになります。

1. Apps Script → 右上 **デプロイ → デプロイを管理**
2. 稼働中デプロイの **✏️（編集）**
3. 設定を確認・変更:
   - **次のユーザーとして実行: 自分**（自分のメールアドレス）
   - **アクセスできるユーザー: 全員** ← ここを「Googleアカウントを持つ全員」から **「全員」** に変更
4. バージョン: **新バージョン** → **デプロイ**

> セキュリティは自前のログイン（ID/PW）で守られます。URLを知っていてもログインしないとデータは一切見えません（全APIがログイン必須）。
>
> ⚠️ 「アクセスできるユーザー」を変更するときは**同じデプロイを編集**してください。「新しいデプロイ」を作るとURLが変わり、メールに載るURLも変える必要が出ます（その場合は設定_外部連携シートの「ダッシュボードURL」も更新）。

---

## コードを更新するとき（毎回の手順）

1. Apps Script で `SeisanDashboard` ファイルの中身を全消し → **SeisanDashboard_コピー用.txt** を貼り付け
2. `dashboard` ファイルの中身を全消し → **dashboard_コピー用.txt** を貼り付け
3. 💾 保存
4. `sd_authorize` を1回実行（権限承認。初回や権限追加時のみ必要）
5. **デプロイ → デプロイを管理 → 編集 → バージョン「新バージョン」→ デプロイ**
6. URL末尾に `?action=ping` を付けて開き、最新バージョン番号が出れば成功

---

## ログインアカウント（本部が発行）

「権限_精算ダッシュボード」シートで管理、または本部ログイン後の **⚙️設定** タブから追加・編集できます。

| ログインID | パスワード | 権限 | 担当店舗 | 有効 |
|---|---|---|---|---|
| honbu | （要変更） | 本部 | 全店 | TRUE |
| （委託先ごとに発行） | 任意 | 委託先 | 担当店舗名 | TRUE |

- **honbu の初期パスワードは必ず変更してください**
- 委託先には「WebアプリのURL」＋発行した「ID/PW」を伝えます（URLは本部と共通。権限で見える範囲が変わります）

---

## 委託先に渡すURL

本部と同じ WebアプリのURL（`.../exec`）を渡します。委託先アカウントでログインすると担当店舗しか見えません。
精算書メール・振込完了メールには、このURLが自動で本文に入ります（ID/PWは載りません）。

---

## 自動で作られる管理シート（すべて編集可）

`設定_精算ダッシュボード`（店舗↔DB対応・必須費目・売上シート店舗名）／ `設定_外部連携`（売上シートID・添付フォルダID・発行元情報・ダッシュボードURL・リマインド送信先）／ `権限_精算ダッシュボード`（アカウント）／ `法人設定_精算ダッシュボード`（振込先口座）／ `定期費目_精算ダッシュボード`（毎月自動セットする費目）／ `振込管理_精算ダッシュボード`（振込済み記録）／ `発行ログ_精算ダッシュボード`（送信・添付履歴）

## 自動処理（「🕘 毎月1日の自動セットをON」ボタンで有効化）

- 毎月1日 朝9時: 前月分（精算対象月）へ定期費目を自動セット
- 毎月18日 朝9時: 振込期限（20日）リマインドを本部（info@ns0314.com）へ送信（入力漏れ疑いの店舗・項目つき）
