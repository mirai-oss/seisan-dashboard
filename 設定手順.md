# 精算ダッシュボード v2 更新手順（所要 約10分）

v1からの追加機能: 💴現金売上の自動取込 ／ 📄PDFプレビュー＆✉️メール送信 ／ 🔁定期費目の自動セット ／ 📎添付アップロード（自動リネーム）／ 🔐ログインアカウント制（本部・委託先）

---

## 1. コードを差し替える（2ファイルとも全置き換え）

1. スプレッドシート → 拡張機能 → Apps Script
2. `SeisanDashboard` ファイルの中身を全部消して、**SeisanDashboard_コピー用.txt** の内容を貼り付け
3. `dashboard` ファイルの中身を全部消して、**dashboard_コピー用.txt** の内容を貼り付け
4. 💾 保存

## 2. sd_authorize を1回実行（権限の承認）

1. エディタ上部の関数選択で `sd_authorize` を選んで **実行**
2. 承認画面が出たら許可（Gmail送信・Drive・外部スプレッドシート読み取りの権限が追加されます）
3. 続けて `sd_diagnose` を実行 → ログで各シートの検出結果を確認

## 3. デプロイ設定の変更 ★重要★

v2は **「次のユーザーとして実行: 自分」** に変更が必要です
（委託先がスプレッドシートの共有なしでログインして使えるようにするため）

1. デプロイ → **デプロイを管理** → ✏️編集
2. 「次のユーザーとして実行」を **自分（自分のメールアドレス）** に変更
3. 「アクセスできるユーザー」は **Googleアカウントを持つ全員** のまま
4. バージョン: **新バージョン** → デプロイ

> もし実行ユーザーの項目が変更できない場合は「新しいデプロイ」を作成してください（URLが変わるので新URLを教えてください）。

## 4. ログインアカウント

初回アクセス時に自動で **「権限_精算ダッシュボード」** シートができます:

| ログインID | パスワード | 表示名 | 権限 | 担当店舗 | 有効 |
|---|---|---|---|---|---|
| honbu | ns0314 | 本部 | 本部 | 全店 | TRUE |
| fam | fam2026 | FAM Dining様 | 委託先 | 秋葉原 肉寿司 | FALSE |

- **honbu のパスワードは必ず変更してください**
- 委託先アカウントは行を追加して発行（担当店舗をカンマ区切りで指定、有効をTRUEに）
- 委託先は: 自店舗の状況・精算書PDF・添付書類の**閲覧のみ**。入力・送信はできません

## 5. 自動でできるシート（すべて編集可）

| シート | 役割 |
|---|---|
| 設定_精算ダッシュボード | 店舗↔DBシート対応、必須費目、**売上シート店舗名**（現金売上取込の対応表） |
| 設定_外部連携 | 売上スプレッドシートID・列指定、添付親フォルダID、メール送信者名 |
| 権限_精算ダッシュボード | ログインアカウント |
| 定期費目_精算ダッシュボード | 毎月自動セットする費目（固定ロイヤリティは前月から自動引き継ぎされるので、それ以外を登録） |
| 発行ログ_精算ダッシュボード | PDF保存・メール送信・添付の履歴（自動記録） |

## 6. 機能の使い方

- **💴 現金売上を取込**: 【サーバー】ダッシュボード「分析_日別店舗」T列の月合計をプレビュー→チェックした店舗に「◯月現金売上」として登録。既に現金売上が入っている店舗は自動でスキップ
- **📄 PDFプレビュー**: 送付シートの精算書をPDF化して画面で確認（保存・送信はしない）
- **✉️ 発行してメール送信**: PDF作成→Drive保存（添付親フォルダ/2026年6月/店舗名/）→メール設定シートの宛先へ送信→発行状況に✅
- **🔁 定期費目をセット**: 前月の固定ロイヤリティ＋定期費目シートの内容を、選択中の月へ自動追加（入力済みはスキップ）
- **🕘 毎月1日の自動セットをON**: 毎月1日 朝9時に前月分（＝精算対象月）へ定期費目を自動セットするトリガーを設定
- **📎 添付**: ファイルを選んで「何の書類か」を入れると `カード売上_2026.06_秋葉原 肉寿司.pdf` のように自動リネームされ、Driveの「2026年6月/店舗名」フォルダに保存

## 注意

- 現金売上の取込には、**デプロイした本人（自分）が【サーバー】ダッシュボードにアクセスできる**必要があります（共有済みならOK）
- メールは**自分のGmailアカウント**から送信されます
- コード更新時は毎回「デプロイを管理 → 編集 → 新バージョン」（新しいデプロイを作るとURLが変わります）
